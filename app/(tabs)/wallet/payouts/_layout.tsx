import { Stack } from "expo-router";
import React from "react";

const PayoutLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default PayoutLayout;
