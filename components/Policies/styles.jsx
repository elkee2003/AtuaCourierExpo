import { StyleSheet, } from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
        marginTop:40,
        marginBottom:20,
        marginHorizontal:10,
    },
    header:{
        fontSize:25,
        fontWeight:'bold',
    },
    subHeader:{
        marginTop:15,
        fontSize:20,
        fontWeight:'bold',
    },
    text:{
        fontSize:15,
        lineHeight:25,
    },
})

export default styles
