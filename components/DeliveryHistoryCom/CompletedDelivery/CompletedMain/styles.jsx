import { StyleSheet,} from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
        marginTop:10,
        marginHorizontal:10,
    },
    title:{
        fontSize:30,
        fontWeight:'bold',
        textAlign:'center',
    }
})

export default styles
